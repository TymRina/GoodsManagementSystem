# 华为云人脸登录配置指南

## 1. 获取华为云API凭证

### 1.1 关于IAM用户和访问密钥

华为云建议**不要直接使用账号（根账号）创建访问密钥**，因为根账号具有所有资源的完全访问权限，一旦泄露会导致严重的安全风险。

**推荐做法**：创建一个具有访问密钥的IAM用户，仅设置为编程访问，并为该用户授予工作所需的最小权限。

### 1.2 创建IAM用户并获取访问密钥

1. 登录华为云控制台：https://console.huaweicloud.com/
2. 点击左上角的服务列表，选择"管理与监管" -> "统一身份认证"
3. 在左侧导航栏中，选择"用户"，然后点击"新建用户"
4. 填写用户信息：
   - 用户名：例如 `face-recognition-user`
   - 访问方式：仅勾选"编程访问"
5. 点击"下一步"
6. 为用户授权：
   - 选择"直接授权"
   - 搜索并添加 `FRS FullAccess` 权限（人脸搜索服务完全访问权限）
   - 可以选择"最小权限原则"，根据实际需求选择更细粒度的权限
7. 点击"下一步"，完成用户创建
8. 在用户列表中，找到刚创建的用户，点击"查看"进入用户详情页
9. 在"访问密钥"标签页中，点击"新增访问密钥"
10. 输入密钥描述，完成身份验证后，下载包含AK和SK的CSV文件

### 1.3 获取PROJECT_ID

#### 方法1：从控制台URL获取（推荐个人用户使用）

1. 登录华为云控制台：https://console.huaweicloud.com/
2. 点击左上角的服务列表，选择任意一个服务，例如"计算" -> "弹性云服务器"
3. 在打开的服务页面中，查看浏览器地址栏中的URL
4. URL中包含类似 `project_id=xxxxxxxxxxxxxxxxxxxxxxxxxxxx` 的参数，复制等号后面的字符串即为PROJECT_ID

#### 方法2：从项目列表获取

1. 登录华为云控制台：https://console.huaweicloud.com/
2. 鼠标移动到右上角用户名，点击"我的凭证"
3. 在项目列表中，复制"项目ID"字段的值

#### 方法3：使用华为云CLI获取

如果您已经安装了华为云CLI，可以通过以下命令获取PROJECT_ID：

```bash
huaweicloud iam project list
```

## 2. 配置人脸登录功能

### 2.1 修改face_compare.py文件

打开 `src/modules/face_compare.py` 文件，修改以下配置：

```python
# 华为云配置
AK = os.getenv("HUAWEICLOUD_SDK_AK","YOUR_AK_HERE")  # 替换为您的AK
SK = os.getenv("HUAWEICLOUD_SDK_SK","YOUR_SK_HERE")  # 替换为您的SK
PROJECT_ID = "YOUR_PROJECT_ID_HERE"  # 替换为您的PROJECT_ID
REGION = "cn-north-4"  # 确保与您的项目区域一致
```

### 2.2 或者设置环境变量

您也可以通过设置环境变量的方式配置AK和SK：

- Windows命令行：
  ```
  set HUAWEICLOUD_SDK_AK=YOUR_AK_HERE
  set HUAWEICLOUD_SDK_SK=YOUR_SK_HERE
  ```

- Windows PowerShell：
  ```
  $env:HUAWEICLOUD_SDK_AK="YOUR_AK_HERE"
  $env:HUAWEICLOUD_SDK_SK="YOUR_SK_HERE"
  ```

## 3. 安装必要的依赖

确保您已经安装了项目所需的依赖：

```
pip install -r requirements.txt
```

## 4. 测试人脸登录功能

### 4.1 确保管理员人脸图片存在

确保 `resources/images/faces/admin.jpg` 文件存在，这是用于比对的管理员人脸图片。

### 4.2 运行项目

```
python main.py
```

### 4.3 测试流程

1. 在登录界面，点击"人脸登录"按钮切换到人脸登录模式
2. 确保摄像头正常工作，您的人脸清晰可见
3. 系统会自动捕获您的人脸并与管理员人脸图片进行比对
4. 如果比对成功，您将成功登录系统

## 5. 常见问题排查

### 5.1 API调用失败

- 检查AK、SK和PROJECT_ID是否正确
- 确保您的项目区域与代码中的REGION一致
- 确保您的账户有足够的余额和权限使用FRS服务

### 5.2 人脸检测失败

- 确保光线充足，您的人脸清晰可见
- 确保摄像头正常工作
- 确保您的人脸在摄像头的拍摄范围内

### 5.3 比对结果不准确

- 确保管理员人脸图片清晰，正面朝向摄像头
- 确保您在登录时的姿势与管理员人脸图片相似
- 可以尝试调整代码中的相似度阈值（默认0.85）

## 6. 进阶配置

### 6.1 更换区域

如果您想使用其他区域的FRS服务，可以修改代码中的REGION参数：

```python
REGION = "cn-north-1"  # 或其他可用区域
```

### 6.2 调整相似度阈值

您可以根据需要调整人脸比对的相似度阈值：

```python
return similarity >= 0.85  # 可以调整为0.9或其他值
```

### 6.3 添加日志记录

代码中已经包含了日志记录功能，您可以调整日志级别来获取更详细的信息：

```python
logging.basicConfig(level=logging.DEBUG)  # 将INFO改为DEBUG获取更详细的日志
```

## 7. 参考文档

- 华为云FRS服务文档：https://support.huaweicloud.com/intl/zh-cn/productdesc-frs/frs_01_0001.html
- 华为云SDK for Python文档：https://support.huaweicloud.com/sdkreference-frs/frs_04_0001.html
- 华为云API Explorer：https://apiexplorer.developer.huaweicloud.com/apiexplorer/doc?product=FRS&api=CompareFaceByBase64